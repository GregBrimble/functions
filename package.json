{
  "name": "functions",
  "scripts": {
    "start": "wrangler pages dev public",
    "start:livereload": "wrangler pages dev public --live-reload",
    "start:kv": "wrangler pages dev public --kv MY_KV_BINDING",
    "help": "wrangler pages dev --help"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.0",
    "typescript": "^4.5.4",
    "wrangler": "^0.0.0-f0a2b68"
  }
}
